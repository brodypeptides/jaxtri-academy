export async function onRequestPost(context) {
  // TODO: affiliate login.
  // Verify email/password, status=approved, role=affiliate or owner.
  return new Response('Affiliate login endpoint scaffold.', { status: 501 });
}
